N = int(input())
regular = set()
vip = set()

for _ in range(N):
    guest_number = input()
    if guest_number[0].isdigit():
        vip.add(guest_number)
    else:
        regular.add(guest_number)

guest = input()
while guest != "END":
    if guest in regular:
        regular.remove(guest)
    else:
        vip.remove(guest)
    guest = input()

print(len(vip) + len(regular))

if vip:
    vip_guests_did_not_come = sorted(list(vip))
    [print(guest) for guest in vip_guests_did_not_come]

if regular:
    regular_guests_did_not_come = sorted(list(regular))
    [print(guest) for guest in regular_guests_did_not_come]
