N = int(input())

matrix = []
sum1 = 0
sum2 = 0

for _ in range(N):
    rows_List = list(map(int, input().split()))
    matrix.append(rows_List)

for i in range(N):
    sum1 += matrix[i][i]

for j in range(N):
    sum2 += matrix[j][N-j-1]

print(abs(sum1-sum2))
