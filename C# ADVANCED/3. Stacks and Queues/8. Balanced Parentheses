using System;
using System.Collections.Generic;

namespace _8._Balanced_Parentheses
{
    class Program
    {
        static void Main(string[] args)
        {
            Stack<char> parantheses = new Stack<char>();
            string inputSequence = Console.ReadLine();
            bool isBalanced = true;

            for (int i = 0; i < inputSequence.Length; i++)
            {
                if (inputSequence[i] == '{' || inputSequence[i] == '[' || inputSequence[i] == '(')
                {
                    parantheses.Push(inputSequence[i]);
                    continue;
                }

                if (parantheses.Count == 0)
                {
                    isBalanced = false;
                    break;
                }

                if (parantheses.Peek() == '(' && inputSequence[i] == ')')
                {
                    parantheses.Pop();
                    continue;
                }
                if (parantheses.Peek() == '[' && inputSequence[i] == ']')
                {
                    parantheses.Pop();
                    continue;
                }
                if (parantheses.Peek() == '{' && inputSequence[i] == '}')
                {
                    parantheses.Pop();
                    continue;
                }
            }

            Console.WriteLine(parantheses.Count == 0 && isBalanced == true ? "YES" : "NO");
        }
    }
}
